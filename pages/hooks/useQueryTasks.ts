import { useRouter } from 'next/router'
import axios from 'axios'
import { useQuery } from '@tanstack/react-query'
import { Task } from '@prisma/client'

export const useQueryTasks = () => {
    return <div>useQueryTasks</div>
}